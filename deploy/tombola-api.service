# systemd unit for the Tombola API in production.
# Replace /path/to/Tombola-app/api below (and the bun binary path — check
# with `which bun`), then:
#   sudo cp tombola-api.service /etc/systemd/system/
#   sudo systemctl daemon-reload
#   sudo systemctl enable --now tombola-api
#   sudo systemctl status tombola-api
#   journalctl -u tombola-api -f          # tail logs

[Unit]
Description=Tombola API
After=network.target

[Service]
Type=simple
WorkingDirectory=/path/to/Tombola-app/api
# `bun run dev` uses --watch, which is a dev-only flag — production runs the
# plain "start" script instead (see api/package.json).
ExecStart=/usr/local/bin/bun run start
Environment=NODE_ENV=production
Restart=on-failure
RestartSec=5
# api/.env is read at process boot via the app's own env loader — no need
# to duplicate secrets here as long as .env lives alongside the service's
# WorkingDirectory.

[Install]
WantedBy=multi-user.target
