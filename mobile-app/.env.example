# ── API ──────────────────────────────────────────────────────────
VITE_API_URL=http://localhost:3435

# ── Chapa ────────────────────────────────────────────────────────
# Public key only (safe to ship client-side) — used by the in-app inline
# checkout widget on the web/PWA surface. From the same Chapa dashboard as
# the api's CHAPA_SECRET_KEY, just the "Public key" field instead.
VITE_CHAPA_PUBLIC_KEY=
